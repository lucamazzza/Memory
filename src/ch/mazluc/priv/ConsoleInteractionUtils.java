package ch.mazluc.priv;

public class ConsoleInteractionUtils {
    // - in: Scanner

    // + readInt(in: Scanner) -> int
    // + readIntInRange(in: Scanner, min: int, max: int) -> int
    // + readNonBlankOrEmptyString(in: Scanner) -> String
    // + isGridSizeValid(in: Scanner, int x, int y) -> int[]
    // + isCoordinateInBounds(in: Scanner, grid: Grid) -> boolean
    // + closeScanner() -> void
    // + clearScanner() -> void
}